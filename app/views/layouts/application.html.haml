!!!
%html
  %head
    %title Twuzz
    =stylesheet_link_tag    'application', :media => 'all'
    =javascript_include_tag 'application'
    =csrf_meta_tags
  %body
    -if current_user
      %div{ :style => 'position:fixed;right:1em;top:1em' }
        =link_to current_user, user_url(current_user)
        %b &nbsp;
        =link_to 'Log out', logout_url

    =yield
